extends Node


"""
#Castle don't deselect on click
building only works from keyboard, after first building, the menu can't be clicked
#Z index sort doesn't always work
if building is placed while turret build menu active, any building gets turret script and runs onto error
"""
	
