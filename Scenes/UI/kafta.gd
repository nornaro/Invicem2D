extends TabContainer

#@onready var kafratab = preload("res://Scenes/UI/kafra_tab.tscn")

#func _ready() -> void:
	#var instance
	#var tabs = Global.RL.get_directories_at("res://Scenes/Building/")
	#for tab in tabs:
		#instance = kafratab.instantiate()
		#instance.name = tab
		#add_child(instance)
	#instance = kafratab.instantiate()
	#instance.name = "Common"
	#add_child(instance)
